# Test
Create an easy to use chatbot that lets admin to add,delete and update user information through natural chat commands

     The system will have a simple chat interface, which would be accessible once the user logs in

     It would have a simple auto login which would let them enter if the user email is added in the system

I made it using following technologies using flask,html,css,python
